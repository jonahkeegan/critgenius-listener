/**
 * @fileoverview Test setup configuration for shared package
 * Configures global test environment and utilities
 */

// Global test setup - can be extended as needed
console.log('🧪 Setting up shared package test environment...');

// Add any global test utilities or configuration here
// For example:
// global.testUtils = { ... };

// Mock global objects if needed for Node.js environment
// global.fetch = vi.fn(); // Example for fetch API mocking
