# Percy Visual Testing Configuration (Client Package)
# ===========================================
# Copy this file to packages/client/.env.percy and update PERCY_TOKEN with your Percy project token.

# Required Percy token
PERCY_TOKEN=your_percy_project_token_here

# Optional overrides
PERCY_BRANCH=main
PERCY_PARALLEL_TOTAL=1
PERCY_LOG_LEVEL=info

# Snapshot behavior
PERCY_DOM_SNAPSHOT_DISABLE_ANIMATIONS=true
PERCY_DOM_SNAPSHOT_DISABLE_FAVICONS=true

# Development convenience
PERCY_ENVIRONMENT=development
PERCY_ALLOWED_HOSTNAMES=localhost,127.0.0.1

# Usage reference
#   pnpm --filter @critgenius/client run test:visual
#   pnpm --filter @critgenius/client run test:visual:baseline
